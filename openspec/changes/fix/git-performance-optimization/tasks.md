# Tasks: fix(git) Performance Optimization

- [ ] **T1**: Aplicar configuraciones de `git config` locales.
- [ ] **T2**: Ejecutar `git gc --prune=now --aggressive` (Mantenimiento pesado).
- [ ] **T3**: Ejecutar `git remote prune origin`.
- [ ] **T4**: Crear script `scripts/git-maintenance.sh` para uso futuro.
- [ ] **T5**: Verificar estado final con `git count-objects -vH`.
