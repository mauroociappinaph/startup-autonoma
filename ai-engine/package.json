{
  "name": "ai-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "ruff check . || echo 'Ruff no instalado'",
    "format": "ruff format . || echo 'Ruff no instalado'",
    "test": "pytest || echo 'Pytest no instalado'",
    "check": "ruff check . || echo 'Ruff no instalado, saltando check de Python'"
  }
}
